from utils.util import get_matrices_from_file

class ExactDXML(object):
	def __init__(self):
		super()

	def fit(train_file):
		total_points, feature_dm, number_of_labels, feature_matrix, label_vectors, label_graph = get_matrices_from_file(train_file, None)